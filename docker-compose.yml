services:
  cf-tunnel-manager:
    build: .
    container_name: cf-tunnel-manager
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./data:/root/.cf-tunnel-manager
    environment:
      - TZ=Asia/Shanghai
      - AUTO_START_TUNNELS=true
